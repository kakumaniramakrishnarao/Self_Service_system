<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/checkQ" method="post">
        <% ques.forEach((i,index)=>{ %>
            <h3><%= i.question %>?</h3>
            <input type="hidden" name="<%= index %>.qid" value="<%= i.quesId %>" >
            <input type="text" name="<%= index %>.ans">
        <% }) %>

        <button type="submit">Submit</button>
   </form>
</body>
</html>